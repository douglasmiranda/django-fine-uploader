============
Installation
============

At the command line::

    $ easy_install django-fine-uploader

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-fine-uploader
    $ pip install django-fine-uploader
